# Session 45 - 2026-05-13

- Module: 模块 2：数据库与 SQLAlchemy
- Concept: 2.6 事务、并发与失败边界

## Summary
继续 2.6b 并发与冲突边界学习，重点观察 lost update 与数据库原子 UPDATE 的区别。通过 `concurrency_examples.py` 演示：两个 session 都读到 `LearningRecord.view_count = 0` 后分别在 Python 中 `+1`，最后只能留下 `1`，这就是 lost update；改用 `SQLAlchemy update(...).values(view_count=LearningRecord.view_count + 1)` 后，加一操作在数据库中完成，两次更新可以得到 `view_count = 2`。

## Covered Concepts
- lost update 的发生机制
- Python 读-改-写与数据库原子 UPDATE 的区别
- 多个 Session 并发修改同一行数据的风险

## Mastered Concepts
- None

## Wins
- 创建并运行 `concurrency_examples.py`
- 观察到 ORM 读-改-写后 `view_count = 1`
- 观察到数据库原子 UPDATE 后 `view_count = 2`

## Struggles
- 还需要继续处理 `IntegrityError -> 409` 的 API 边界
- 2.6 的并发和冲突处理还需要再用 FastAPI demo 串起来

## Next Session
继续 2.6b：围绕 `User.name` 唯一约束冲突，练习把数据库 `IntegrityError` 转换为业务异常，再由 FastAPI API 返回 `409 Conflict`。
